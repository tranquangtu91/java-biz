spring:
  application:
    name: biz

---
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/base_app
    username: root
    password: tutq@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# ---
# spring:
#   jackson:
#     serialization:
#       write-dates-as-timestamps: false
---
log:
  app_name: base-app
  pattern: "%d{yy-MM-dd HH:mm:ss.SSS, +07:00} [%5p] %C{5}.%M.%L: %m%n"
  logstash:
    destination: localhost:5000
  file:
    path: "/opt/logs"
---
logging:
  level:
    com:
      zaxxer: ERROR
    org:
      apache: ERROR
      hibernate: ERROR
      springframework: ERROR
